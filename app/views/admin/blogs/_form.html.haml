= simple_form_for @blog || Blog.new, :url => admin_blogs_path do |f|
  = f.input :title, :required => false
  = f.input :description, :required => false
  = f.input :show_fork, :as => :boolean, :label_html => {:class => :fork_me}
  
  = f.submit t('.save')
